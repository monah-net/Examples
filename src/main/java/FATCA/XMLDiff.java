package FATCA;

/*import org.xmlunit.builder.DiffBuilder;
import org.xmlunit.diff.Diff;

public class XmlDiff {
    public static boolean compareXml(String file1, String file2) {
        try {
            Diff xmlDiff = DiffBuilder.compare(file1)
                    .withTest(file2)
                    .ignoreWhitespace()
                    .checkForSimilar()
                    .build();

            return !xmlDiff.hasDifferences();
        } catch (Exception e) {
            e.printStackTrace();
            return false;
        }
    }
}*/
